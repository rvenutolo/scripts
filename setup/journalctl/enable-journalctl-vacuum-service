#!/usr/bin/env bash

set -euo pipefail

source "$(dirname -- "${BASH_SOURCE[0]}")/../functions.bash"
check_not_root
check_no_args "$@"

readonly service_desc='journalctl-vacuum'
readonly service_unit='journalctl-vacuum.timer'

enable_user_service "${service_desc}" "${service_unit}"
