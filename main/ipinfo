#!/usr/bin/env bash

set -euo pipefail

source "$(dirname -- "${BASH_SOURCE[0]}")/../lib/functions.bash"
check_no_args "$@"

curl --silent --show-error --location ipinfo.io \
  | jq 'del(.readme)' \
  | jq -r 'to_entries[]|"\(.key)|\(.value)"' \
  | column --table --separator '|'
