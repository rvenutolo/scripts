#!/usr/bin/env bash

set -euo pipefail

#shellcheck disable=SC1091
source "${SCRIPTS_DIR}/functions.bash"
check_at_most_1_arg "$@"

if [[ "$#" -eq 0 ]]; then
  download "wttr.in/${WTTR_CITY}"
else
  download "wttr.in/$1"
fi
