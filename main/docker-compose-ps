#!/usr/bin/env bash

# @description Run `docker compose ps` in the current directory.
# @noargs
# @stdout Compose service status table.

set -Eeuo pipefail
IFS=$'\n\t'

# shellcheck disable=SC1091 # functions.bash is not statically followable
source "${SCRIPTS_DIR}/functions.bash"
log::enable_err_trap
args::handle_help_flag "$@"
system::require_bash_version 4 0
args::check_no_args "$@"

docker compose ps
