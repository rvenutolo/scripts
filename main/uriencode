#!/usr/bin/env bash

set -euo pipefail

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &> /dev/null && pwd)/../lib/functions.sh"
check_exactly_1_arg "$@"

jq --null-input --raw-output --arg v "$1" '$v|@uri'
